{
  "name": "my-article-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node build.js --watch --serve",
    "build": "node build.js",
    "clean": "rm -rf dist && mkdir -p dist",
    "deploy": "firebase deploy --only hosting"
  },
  "dependencies": {
    "ejs": "^3.1.9",
    "fs-extra": "^11.1.1",
    "gray-matter": "^4.0.3",
    "marked": "^9.0.0",
    "live-server": "^1.2.2"
  }
}
